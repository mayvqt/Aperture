# Development

Use the [development task index](development/README.md) to find ownership,
architecture, data, validation, operations, and documentation guidance.

For a local server:

```bash
go run ./cmd/aperture serve
```

Open `http://localhost:8099/setup`. Go does not load `.env` automatically.
